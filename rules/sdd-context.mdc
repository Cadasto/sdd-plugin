---
description: Spec-Driven Development — the spec is the source of truth; keep document kinds separate, identifiers stable, and the traceability chain intact. The plugin owns the spec/document layer and the delivery pipeline that runs on it. Mirrors the spec-driven-development router skill for Cursor.
globs: ["docs/**/*.md", "docs/**/*.yaml", "AGENTS.md"]
alwaysApply: false
---

# Spec-Driven Development

Apply when working in this repo's `docs/` tree or `AGENTS.md`. Read `docs/.sdd.yaml` first.

**The specification — not the code, not the prompt — is the source of truth.** The plugin owns the spec/document layer and the delivery pipeline that runs on it — plan, workers, review, triage, close-out. A general engineering plugin is optional, and only at the exploration end.

**SDD commands:** `/sdd-scaffold` · `/sdd-specify` · `/sdd-deliver` · `/sdd-review` · `/sdd-triage` · `/sdd-trace` · `/sdd-archive` · `/sdd-finalize`. For the full flow, use the `spec-driven-development` skill or — in a scaffolded repo — [docs/ai-workflow.md](docs/ai-workflow.md).

**Load-bearing rules:** one canonical home per requirement (index links, spec owns the prose); identifiers immutable once published; don't mix document kinds; don't settle open questions silently (STRAND, ADR, or ask); look domain facts up in the named ground-truth source. No code-first without a `REQ`/spec — `/sdd-specify` first. Plans live in `docs/plans/`, on the branch. One home per fact in process prose too — commit body / PR body / changelog / review comments cite identifiers rather than restating. Flip the plan to `status: done` **in place** inside the implementing PR — no move, no index; finished plans are swept at the next version bump. Findings live in one numbered ledger on the PR, never one comment per finding. Every `docs/` document declares its kind; generated blocks are written by `sdd-check generate`; the gate is `spec-check`.
